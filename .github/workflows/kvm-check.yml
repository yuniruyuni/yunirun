name: KVM check
on: workflow_dispatch
permissions: {}
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: KVM の有無
        run: |
          ls -l /dev/kvm 2>&1 || echo "no /dev/kvm"
          grep -c -E 'vmx|svm' /proc/cpuinfo || true
          nproc; free -h | head -2
